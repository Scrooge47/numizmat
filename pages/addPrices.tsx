import * as React from 'react';
import AddPricesForm from 'src/components/addPrices';
interface Props {}
export const addPrices = (props: Props) => {
	return (
		<>
			<AddPricesForm />
		</>
	);
};

export default addPrices;
